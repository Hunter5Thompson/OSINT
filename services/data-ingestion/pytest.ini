[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -m "not live"
markers =
    integration: requires local dev-compose services
    live: touches an external GDELT or model-serving runtime
    slow: backfill or performance tests
