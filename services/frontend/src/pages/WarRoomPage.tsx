/**
 * War Room placeholder — full implementation lands in Sprint 4.
 */
export function WarRoomPage() {
  return (
    <div
      style={{
        flex: 1,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: "'Instrument Serif', serif",
        fontStyle: "italic",
        fontSize: "1.5rem",
        opacity: 0.75,
      }}
    >
      § War Room · pending sprint 4
    </div>
  );
}
